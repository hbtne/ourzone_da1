
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        top: 22, // Điều chỉnh vị trí này tùy theo yêu cầu
        right: 15, // Đặt nút ở phía bên phải
        zIndex: 10,
    },
});

export default styles;
